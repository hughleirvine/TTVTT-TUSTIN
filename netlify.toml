# This is the essential plugin for deploying Next.js sites on Netlify.
# It handles all build commands and configurations automatically.
[[plugins]]
  package = "@netlify/plugin-nextjs"

# This section sets a longer timeout for ALL server-side functions.
# This is now necessary for the Puppeteer scraper to have enough time to run.
[functions]
  timeout = 25